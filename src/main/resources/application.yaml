spring:
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  jackson:
    time-zone: UTC
mail:
  config:
    host: smtp.gmail.com
    port: 587
    address: praktyki2019tt@gmail.com
    password:
server:
  default:
    timezone: UTC
  servlet:
    context-path: /api